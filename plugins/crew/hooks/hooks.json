{
  "description": "Stops the Gunner discarding the developer's uncommitted work with git checkout, restore, stash, reset, clean or switch. Acts only inside the gunner subagent.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "PY=\"$(command -v python3 || command -v python)\"; \"$PY\" \"${CLAUDE_PLUGIN_ROOT}/hooks/guard-git.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
